{% set directive_new = directive|replace({'-':'_'}) %}
<div class="{{ class }}" {{ directive }} ng-cloak ng-init="uuid_{{ directive_new }} = '{{ uuid }}'">
    <div class="card-block">
        <div class="row">
            <div class="col s12">
                <div class="card clearfix">
                    <h2>Formulario para carga de imágenes</h2>
                    {{ form }}
                </div>
            </div>
        </div>
    </div>
</div>